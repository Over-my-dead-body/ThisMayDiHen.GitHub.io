#include<stdio.h>
int main(){
	int i=0;
	for(i=1;i<=9;i++){
		int j=0;
		for(j=1;j<=i;j++){
			printf("%d*%d=%-2d ",i,j,i*j);
			//%-2d是指：左对齐且本位占两个空间（两个空间且短者选择靠左位置）
			//%2d是指：右对齐且本位占两个空间（两个空间且短者选择靠右位置）
			//具体样式可以自己改着看看 
		}
		printf("\n");
	}
	return 0;
}
