#include<stdio.h>
int main(){
	int arr[10]={1,2,8,4};
  //数组名arr只有在两种情况下表示数组：1.sizeof时 2.&arr取地址时
	printf("%p\n",arr);
	printf("%p\n",&arr[0]);
	//--------注意！--------- 
	printf("%p\n",&arr); 
	//&arr代表的是数组的地址！与arr，&arr[0]所代表的数组首元素地址完全不相同！
	//具体证据就是，如若给以上三个加一，得出的地址完全不同
	// arr，&arr[0]的地址将是数组第二个元素地址，而&arr的地址将是整个数组后面的地址！！（一下跳一个和一下跳一组） 
	//-------注意！---------- 
	printf("%d\n",*arr);
	//最后这条是对arr解引用，解出来其值为一，也就是数组首元素 
	return 0;
}
